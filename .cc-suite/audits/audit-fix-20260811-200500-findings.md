# Audit Findings

**Run**: audit-fix 20260811-200500 | **Scope**: the 7 commits since 9a47e5b (docling/marker settings, marker backend, bake-off checkpoint/resume, CLI tier fix, judge.hurdle, doc rewrites) | **Audit type**: full (9-dim)
**Model**: gpt-5.6-sol | **Effort**: high | **Sandbox**: read-only | **Audit thread**: 019ff0a9
**Jobs**: 5 launched. pdf/settings/bakeoff/tests completed; cli STALLED at 10 min, was relaunched at 25 min, and completed — its findings are rows appended at the end.
**Status values**: open | fixed | not-fixed | partial | regressed | skipped (severity filter) | skipped (user stop)

Two independent jobs (bakeoff, tests) reproduced the SAME Critical defect — partial abstention
still wins a ranking. Agreement between two finders on a *reproduced* defect is the strongest
signal in this file.

| # | File | Line | Severity | Dimension | Finding | Suggested fix | Status | Round |
|---|------|------|----------|-----------|---------|---------------|--------|-------|
| 1 | eval/extract_bakeoff.py | 163 | Critical | Code Correctness & Reliability | Resume compatibility checks only seed and sample size. Changed tiers, PDF contents/paths, anchors, backend configuration, OCR tools, or code are silently mixed with old measurements; documents are then skipped solely by basename. A request to evaluate a newly added tier can return an old ranking without running that tier. | Persist and validate an experiment manifest containing ordered tiers, canonical PDF identities plus content hashes, anchor hash, backend/config fingerprints, and relevant executable versions before accepting a checkpoint. | fixed | 1 |
| 2 | eval/extract_bakeoff.py | 227 | Critical | Code Correctness & Reliability | Failed or unavailable tier/document pairs are omitted entirely, and `a["pdfs"]` counts only successful rows. A backend measured perfectly on 1/20 PDFs can therefore outrank one measured well on 20/20 while being printed as `scored 1/1`. I reproduced `sparse` ranking above `full` despite failing half the corpus. This is a confident verdict on unmeasured evidence. | Record every requested tier/document pair, distinguish extraction failure from visual abstention, require adequate paired coverage across the same documents, and refuse to rank otherwise. | fixed | 1 |
| 3 | tests/test_bakeoff.py | 121 | Critical | Testing & Validation | The test covers only a backend with zero measurements. It does not cover partial abstention. Production currently ranks a backend scoring 1.0 on one PDF and abstaining on another above a backend scoring 0.9 on both; I reproduced `['sparse', 'complete']`. This turns incomplete evidence into a confident ranking. | Add a multi-document test with partial abstention and require complete/equivalent measurement coverage, a minimum coverage threshold, or an explicitly unranked result. | fixed | 1 |
| 4 | eval/extract_bakeoff.py | 150 | High | Code Correctness & Reliability | `--resume` silently starts a fresh experiment when the checkpoint is absent, unreadable, or corrupt. Line 155 deliberately suppresses the condition, contrary to “fail loud, fail closed,” and can overwrite the damaged evidence after an expensive rerun. | When resume was requested, require a valid compatible checkpoint and exit nonzero otherwise; make restarting an explicit separate action. | fixed | 1 |
| 5 | eval/extract_bakeoff.py | 178 | High | Code Correctness & Reliability | Remote docling and marker are converted twice: `_extract()` obtains text, then `_structured_pages()` runs the backend again at line 191. Word counts/agreement and visual fidelity can therefore describe different nondeterministic executions. The second call also doubles the most expensive work. | Create one structured extraction result containing text and page text and reuse it for every metric. | fixed | 2 |
| 6 | eval/extract_bakeoff.py | 200 | High | Code Correctness & Reliability | Fidelity and order are reduced to medians twice—across pages and then PDFs. A backend can completely corrupt almost half its sampled pages/documents and still receive a perfect median. In particular, line 244 contradicts its comment: a measured inverted page does not demote the backend unless the median order becomes negative. | Preserve the distribution and rank using paired acceptance criteria such as minimum measured share, worst/lower quantile thresholds, and confidence intervals. Treat any configured gross-inversion threshold breach explicitly. | fixed | 3 |
| 7 | eval/extract_bakeoff.py | 272 | High | Security & Risk Management | The output path is not checked against input PDFs or the anchors file. `--out` naming an input PDF causes `_checkpoint()` to overwrite that PDF; in a multi-file run it can destroy a later input before it is processed. | Resolve all paths before work begins and reject output/temp paths equal to, contained by, or aliasing any input or anchors path. | fixed | 1 |
| 8 | eval/extract_bakeoff.py | 68 | High | Code Correctness & Reliability | Every `pdfinfo` error, timeout, nonzero result, malformed output, or missing binary becomes an unexplained page count of zero. Sampling then selects no pages, all visual measurements abstain, and line 247 can still emit an alphabetical “ranking.” | Return page count plus explicit error, check the subprocess return code, and fail the experiment or suppress ranking when page sampling cannot be established. | fixed | 1 |
| 9 | klode/lib/formats/pdf.py | 100,219 | High | Security & Risk Management | Endpoint resolution accepts credential-bearing URLs such as `http://user:password@host` because validation checks only the scheme prefix. This permits credentials in `settings.toml`, directly violating “credentials are never settings.” | Parse URLs with `urllib.parse.urlsplit`; reject nonempty username/password, missing hosts, fragments, and malformed ports in the common settings validator. | fixed | 1 |
| 10 | klode/lib/formats/pdf.py | 348 | High | Code Correctness & Reliability | `_better` rejects only empty output despite claiming to reject “EMPTY/short” output. A one-word, zero-corruption OCR result replaces hundreds of words and suppresses Docling escalation. This was reproduced: `auto` selected a one-word Xberg result. | Apply `MIN_WORDS` or a retention/coverage ratio to every candidate, not only the initial `pdftotext` fast path. | fixed | 1 |
| 11 | klode/lib/formats/pdf.py | 365,374 | High | Testing & Validation | Xberg and Docling occupy the automatic ladder without a committed measurement demonstrating that they outperform the tier they displace. The repository contains the bake-off harness but no result artifact establishing these ladder choices; mocked “clean text” tests are not comparative evidence. | Remove unmeasured backends from `auto` until a reproducible bake-off report supports them, or commit the corpus, configuration, results, and acceptance threshold that earned each position. | fixed | 2 |
| 12 | klode/lib/formats/pdf.py | 385 | High | Code Correctness & Reliability | `auto` returns `best` even when every candidate failed its own usability criteria. A garbled or sub-`MIN_WORDS` `pdftotext` result can therefore succeed after OCR is unavailable; downstream verification abstains because `pdftotext` is also the control, allowing default ingestion to promote known-bad text. | Raise an extraction error when no candidate satisfies the final usability threshold; require an explicit forced tier/override to retain rejected text. | fixed | 3 |
| 13 | tests/test_bakeoff.py | 173 | High | Code Correctness & Reliability | The unreachable-endpoint test asserts only `visual is None`. It passes even if structured-page lookup is deleted entirely, and production swallows the endpoint error before misreporting “no page separators.” That is silent, misleading failure. | Assert the lookup was called and that the row records the actual endpoint failure separately from absent pagination. | fixed | 3 |
| 14 | tests/test_bakeoff.py | 233 | High | Testing & Validation | Resume compatibility is tested only for seed and sample size. Production accepts changed tiers, anchors, PDF identities/content, or scoring-version semantics and merges them into one report. | Persist and validate tiers, anchor hash, input path/content fingerprints, and report/schema/scoring version; add rejection tests for each mismatch. | fixed | 1 |
| 15 | tests/test_bakeoff.py | 241 | High | Code Correctness & Reliability | The test explicitly blesses silently restarting from a corrupt checkpoint. With `resume=True`, corruption should fail loud and closed; restarting can overwrite the only partial report and silently redo or mix an expensive experiment. | Expect `SystemExit`/a dedicated checkpoint error, preserve the corrupt file, and require an explicit restart option to discard it. | fixed | 1 |
| 16 | tests/test_formats.py | 423 | High | Testing & Validation | The Docling transport test checks markdown, URL, and filename but never verifies that structured JSON provenance is requested or propagated. Removing the `json` format request would leave Docling unmeasurable while this test and the mocked bake-off provenance test remain green. | Assert repeated `to_formats=md` and `to_formats=json` fields, return realistic `json_content`, and verify page provenance reaches `Choice.pages` and page text. | fixed | 3 |
| 17 | tests/test_settings.py | 333 | High | Security & Risk Management | `test_no_credential_is_read_from_the_environment_by_this_module_either` inspects only `SPEC.env`. Direct `os.environ`/`getenv` credential reads elsewhere in `settings.py` would pass. Its denylist also misses common names such as `auth`, `bearer`, `access_key`, and `cookie`. | Enforce the architectural rule over executable module code, preferably with an AST/import-boundary check, and use a comprehensive credential classification rather than this substring list alone. | fixed | 3 |
| 18 | eval/extract_bakeoff.py | 143 | Medium | Security & Risk Management | The predictable `<out>.part` checkpoint follows pre-existing symlinks and is shared by concurrent runs. In an attacker-writable directory, a prepared symlink can cause arbitrary user-writable file corruption; concurrent writers can also clobber or rename each other’s temporary file. | Create a unique same-directory temporary file with exclusive creation, restrictive permissions, flush/fsync it, then atomically replace the destination. | fixed | 1 |
| 19 | eval/extract_bakeoff.py | 172 | Medium | Code Correctness & Reliability | PDFs are identified only by `pdf.name`. Two files such as `a/report.pdf` and `b/report.pdf` overwrite each other at line 221; on resume the second is skipped as already complete. | Use canonical relative paths or stable content-derived IDs as report keys and reject duplicate identities. | fixed | 1 |
| 20 | eval/extract_bakeoff.py | 198 | Medium | Performance & Efficiency | The identical rendered-page OCR truth is recomputed inside the tier loop. With four tiers, every sampled page is rendered and OCRed four times rather than once. | Render/OCR sampled pages once per PDF, cache the reference tokens, and compare every tier against that immutable reference. | fixed | 3 |
| 21 | eval/extract_bakeoff.py | 198 | Medium | Testing & Validation | Reinitializing sampling with the same seed for every PDF means every equal-length document samples identical page positions. Common document structure can systematically exclude the same sections across the corpus, violating the independence expected of a statistical sample. | Derive a deterministic per-document seed from the global seed and stable document identity, while recording both seed and selected pages. | fixed | 3 |
| 22 | eval/extract_bakeoff.py | 200 | Medium | Code Correctness & Reliability | The code tests `vr.ran`, not `vr.measured`, and discards every `PageCheck`. When rendering ran but every sampled page failed or was unavailable, `visual` becomes `None` without a note and the actual per-page errors disappear. | Check `vr.measured`, serialize attempted/measured counts and each page error, and attach a mandatory abstention reason whenever no score exists. | fixed | 1 |
| 23 | eval/extract_bakeoff.py | 212 | Medium | Code Correctness & Reliability | Agreement telemetry omits the available `control_pages`, so its order windows are fixed-size proxies that can hide page-local reversal. Line 215 then labels nearest-rank `quantile(0.5)` as `order_median`, despite `Agreement.median` explicitly providing the true median. | Pass `coverage.split_pages(control_raw)` to `agreement.compare()` and store `agr.median`. | fixed | 1 |
| 24 | eval/extract_bakeoff.py | 218 | Medium | Code Correctness & Reliability | Anchor JSON has no schema validation. A string value is iterated character-by-character as anchors, and wrong-shaped or unmatched mappings can silently produce no compatibility metric. The help at line 262 incorrectly says `card_stem`, while lookup uses `pdf.stem`. | Validate `dict[str, list[str]]`, reject empty/non-string phrases and unmatched keys, and document the key as `pdf_stem`. | fixed | 1 |
| 25 | eval/extract_bakeoff.py | 260 | Medium | Code Correctness & Reliability | CLI inputs are insufficiently validated: zero/negative `--sample`, empty `--tiers`, whitespace-only tier names, and a nonexistent/non-file `--pdfs` path can all produce an empty or meaningless report with exit status 0. | Add a positive-integer parser, normalize and validate a nonempty unique tier list, require existing regular PDF files, and exit nonzero when nothing can be measured. | fixed | 1 |
| 26 | klode/lib/formats/pdf.py | 126,261 | Medium | Performance & Efficiency | Both remote paths load the entire PDF into memory and then construct another complete multipart byte string. A large or hostile PDF can require multiple times its size in memory; only response size is capped. | Enforce an input-size limit and stream multipart uploads instead of using `Path.read_bytes()` plus `bytes.join`. | fixed | 3 |
| 27 | klode/lib/formats/pdf.py | 140,279,282 | Medium | Code Correctness & Reliability | Nested remote-response types are not validated. Truthy non-dict `document`/`metadata` values or non-string markdown/output values raise `AttributeError`, contradicting the promised stable `RuntimeError` handling and bypassing selected auto fallback catches. | Validate every consumed field’s type before calling `.get()` or `.strip()` and normalize all malformed schemas to a backend-specific `RuntimeError`. | fixed | 3 |
| 28 | klode/lib/formats/pdf.py | 174,291 | Medium | Performance & Efficiency | `docling_page_text` and `marker_page_text` perform a fresh full conversion. Callers such as the bake-off first extract text and then request page text, uploading and converting each PDF twice—especially costly for Marker’s minutes-long conversions. | Expose one structured conversion result containing markdown, pages, and page text; reuse it throughout extraction and evaluation. | fixed | 3 |
| 29 | klode/lib/formats/pdf.py | 195 | Medium | Code Correctness & Reliability | A blanket `except Exception: pass` suppresses failures while configuring full-page Tesseract OCR. Missing binaries, invalid configuration, backend API changes, and programming errors silently fall back to different OCR behavior. | Catch only the documented compatibility exception; otherwise raise a backend-specific error with the original cause. | fixed | 3 |
| 30 | klode/lib/formats/pdf.py | 244 | Medium | Code Correctness & Reliability | Duplicate Marker page IDs overwrite earlier page text in the dictionary. When `page_stats` is absent, contradictory pagination can be reported as valid page provenance. | Detect an already-seen page number and return unknown provenance or raise a malformed-response error. | fixed | 3 |
| 31 | klode/lib/formats/pdf.py | 351 | Medium | Code Correctness & Reliability | The code uses `score <= cur.score` although the adjacent contract says “strictly-lower.” An equally scored installed backend displaces the current extraction without measuring better; repeated ties can automatically select Docling. | Use `<`, then define an explicit, evidence-based tie breaker if one is required. | fixed | 1 |
| 32 | klode/lib/formats/pdf.py | 45,58,177,294 | Medium | Code Correctness & Reliability | The public `lang` argument is ignored by every extractor. This contradicts the CLI contract that `--lang` controls Tier 2/3 OCR and can silently OCR non-English documents with the wrong language. | Wire `lang` into Kreuzberg, local Docling/Tesseract, and supported remote request fields, or remove the option and correct its documentation. | fixed | 3 |
| 33 | klode/lib/formats/pdf.py | 64,130,198,265 | Medium | Security & Risk Management | OCR conversion has no reliable wall-clock bound. Local Xberg and Docling have no timeout, while `urlopen(timeout=...)` bounds individual socket operations rather than total slow-drip response time. A hostile PDF or endpoint can wedge ingestion indefinitely. | Isolate local backends in cancellable worker processes and enforce total deadlines for both local and remote conversion. | fixed | 3 |
| 34 | klode/lib/settings.py | 124 | Medium | Code Correctness & Reliability | `Settings.value()` and `source()` silently convert an unknown programmatic key into `None`/`"unset"`. A typo such as `ingest.verfy` is therefore indistinguishable from a deliberately unset setting and can silently disable safety behavior. | Use `self[name]` so unknown names raise `KeyError`, or add an explicit `default` parameter when permissive lookup is genuinely required. | fixed | 3 |
| 35 | klode/lib/settings.py | 143 | Medium | Code Correctness & Reliability | `Path.is_file()` collapses missing files, directories, and some stat/permission failures into the same empty configuration. Confirmed: passing `explicit="klode/lib"` returns `{}`. An explicitly requested missing path is also silently ignored. | Attempt to open the path directly; suppress only `FileNotFoundError` for the implicit default path. Reject missing explicit paths, directories, and permission failures with `ValueError`. | fixed | 3 |
| 36 | klode/lib/settings.py | 155 | Medium | Code Correctness & Reliability | Unknown sections are detected only while iterating their keys. An empty typoed table such as `[ingset]` therefore returns `{}` without error, despite the unknown-setting guarantee. | Validate every top-level section against the set of known sections before iterating its contents; add an empty-unknown-section regression test. | fixed | 3 |
| 37 | klode/lib/settings.py | 172 | Medium | Code Correctness & Reliability | An explicitly present but empty environment variable is treated as absent and silently falls through to file/default. This violates fail-loud behavior and can conceal broken deployment configuration. | Treat only a missing variable as absence; reject an empty value with a setting-specific error. Users can remove the variable when they intend no override. | fixed | 3 |
| 38 | klode/lib/settings.py | 202 | Medium | Security & Risk Management | URL validation checks only a prefix. It accepts credential-bearing URLs such as `https://user:password@host` and `https://host/?api_key=secret`, contradicting the “credentials are never settings” rule and exposing them through settings output/backups. It also accepts unusable values such as `http://`. | Parse with `urllib.parse.urlsplit`; require a valid hostname and port, reject control characters, userinfo, query credentials, and fragments, and test those cases. | fixed | 1 |
| 39 | klode/lib/settings.py | 257 | Medium | Code Correctness & Reliability | File values are validated only when they win precedence. A malformed file value is silently ignored while an environment or argument override exists. Confirmed: `ingest.verify="not-a-boolean"` resolved successfully when the environment supplied `true`. This contradicts lines 139–141 and 190–192, which promise malformed files and every source are validated. | Validate every supplied file entry immediately after loading or injecting `file_values`, before applying precedence. Decide and document whether shadowed environment values receive the same treatment. | fixed | 3 |
| 40 | klode/lib/settings.py | 84 | Medium | Testing & Validation | The safety-relevant consumer wiring for `ingest.tier`, `ingest.verify`, and `ingest.marker_mode` lacks end-to-end assertions. Resolver tests would still pass if `ingest.run()` stopped forwarding tier/verify or the marker request stopped sending mode. | Mock the consumer boundaries and assert file/environment values reach `ingest.ingest(tier=..., verify=...)` and marker multipart field `mode`. | fixed | 3 |
| 41 | klode/lib/settings.py | 91 | Medium | Security & Risk Management | Both remote extraction endpoints permit plaintext HTTP. Klode subsequently uploads complete PDFs to these endpoints, so non-loopback HTTP exposes document contents to interception; binding a service to a private interface does not provide transport confidentiality. | Require HTTPS for non-loopback endpoints, or require an explicit insecure-transport opt-in with a prominent warning. | fixed | 3 |
| 42 | tests/test_bakeoff.py | 129 | Medium | Testing & Validation | `test_the_reported_median_is_a_median_not_the_upper_of_two` tests `_median()` in isolation. It would still pass if production aggregation stopped using `_median()` or reported another statistic. | Exercise `bake_off()` with two documents producing known scores and assert the resulting `aggregate[*]["median_visual"]` and `median_order`. | fixed | 1 |
| 43 | tests/test_bakeoff.py | 223 | Medium | Code Correctness & Reliability | All resume fixtures have unique basenames. Production keys reports and `done` by `pdf.name`; two PDFs from different directories with the same name overwrite one another. I reproduced a two-input report containing only `['same.pdf']`. | Key by a stable relative/absolute identity or content hash and add duplicate-basename and resume tests. | fixed | 2 |
| 44 | tests/test_bakeoff.py | 26 | Medium | Dependency & Environment Safety | `HAVE_POPPLER` checks only `pdftotext` and `pdfinfo`, but the corpus score also requires `pdftoppm` and `tesseract`. The class runs and fails on partial installations instead of skipping accurately. | Include `visual.available()` in the corpus guard; keep the CLI wording test independent by mocking the bake-off. | fixed | 3 |
| 45 | tests/test_bakeoff.py | 276 | Medium | Testing & Validation | The seed assertion checks only the input echoed at report top level; the fake does not capture the seed passed to visual measurement. The sampled-page assertion checks only length. Using the wrong measurement seed or fabricating any two page numbers would pass. | Capture and assert the exact `seed`, exact `pages`, and exact persisted `visual_sampled` against `visual.sample_pages(...)`. | fixed | 3 |
| 46 | tests/test_bakeoff.py | 299 | Medium | Testing & Validation | `test_it_runs_and_scores_the_corpus` only asserts that at least one of five PDFs scored. It still passes if scoring is deleted or broken for four PDFs, so it does not test the behavior its name claims. | Assert that every expected PDF has a measured row, the measured count equals the corpus size, and there are no unexpected skips or abstentions. | fixed | 2 |
| 47 | tests/test_formats.py | 151 | Medium | Dependency & Environment Safety | The zero-dependency import test uses a four-package denylist. Adding any other runtime dependency—such as `requests`, `pypdf`, or a new backend—would pass. | Run the import in an isolated stdlib-only environment or fail imports outside stdlib and the project namespace. | fixed | 3 |
| 48 | tests/test_formats.py | 258 | Medium | Testing & Validation | `test_zip_traversal_refused_and_nothing_written` observes an unrelated empty sentinel directory. A defective extractor could write the traversal target elsewhere and then raise, and the “nothing written” assertion would still pass. | Snapshot the entire temporary root and relevant parent before/after, or patch all filesystem write/extract APIs and assert they were never called. | fixed | 3 |
| 49 | tests/test_formats.py | 355 | Medium | Testing & Validation | `test_auto_degrades_when_ocr_absent` codifies the unsafe fail-open: it expects a known-garbled `pdftotext` result to succeed when OCR is missing. | Replace it with an assertion that `auto` fails closed, while separately testing that a forced tier can retain the result explicitly. | fixed | 3 |
| 50 | tests/test_formats.py | 380 | Medium | Security & Risk Management | The subprocess test covers a nonzero exit but not the hostile/wedged-PDF timeout. Removing the timeout or failing to translate `TimeoutExpired` would not be caught. | Assert the exact `timeout=PDFTOTEXT_TIMEOUT` call and test that `TimeoutExpired` becomes the documented `RuntimeError`. | fixed | 3 |
| 51 | tests/test_formats.py | 386 | Medium | Testing & Validation | The auto-Docling test mocks `_docling` and checks only the handler. It would still pass if automatic page-provenance propagation were deleted and production reverted to the text-only extractor. | Mock `_docling_with_pages` directly and assert the exact `pages` value on the resulting extraction. | fixed | 3 |
| 52 | tests/test_formats.py | 386 | Medium | Dependency & Environment Safety | Auto-tier tests patch `_docling`, but production first consults `_docling_with_pages` and configured endpoints. A developer’s environment or `~/.klode/settings.toml` can bypass the mock and perform a real network call. | Patch `_docling_with_pages` directly and isolate both environment and settings path in every auto-tier test. | fixed | 3 |
| 53 | tests/test_formats.py | 394 | Medium | Testing & Validation | `assertIn(e.handler, ("pdftotext", "xberg"))` permits the worse-scoring Xberg result to replace the better PDF text. Deleting the score comparison in `_better` would still pass despite the “best available” comment. | Assert the exact expected winner and text for deliberately ordered corruption scores. | fixed | 1 |
| 54 | tests/test_formats.py | 448 | Medium | Dependency & Environment Safety | Removing `KLODE_DOCLING_URL` does not ensure “no endpoint”: a user settings file can still configure one. The test can make a live remote request instead of exercising the local ImportError path. | Patch `docling_endpoint()` to return `None` and patch the import seam; do not depend on the caller’s home configuration. | fixed | 3 |
| 55 | tests/test_formats.py | 462 | Medium | Performance & Efficiency | Only Docling response size is tested. The adapter reads the entire input PDF and constructs another full multipart copy without an input cap, so a large untrusted PDF can exhaust memory before the response guard matters. | Enforce a documented maximum input size or stream uploads, and add boundary tests immediately below and above the limit. | fixed | 3 |
| 56 | tests/test_formats.py | 507 | Medium | Testing & Validation | The real `pdftotext` integration test uses a deliberately trivial/invalid PDF and converts any `RuntimeError` into a skip. It remains green if the integration is permanently broken. | Use the repository’s valid PDF fixture and assert known extracted text; skip only when the executable is absent. | fixed | 3 |
| 57 | tests/test_formats.py | 507 | Medium | Testing & Validation | The purported real Poppler integration catches every `RuntimeError` and converts it to a skip. A completely broken `_pdftotext` implementation that always raises therefore passes the suite. | Use a valid committed PDF fixture and let extraction failure fail; skip only when the executable is absent before invoking production. | fixed | 3 |
| 58 | tests/test_formats.py | 613 | Medium | Performance & Efficiency | Marker has the same response-only size test and unbounded input-body construction as Docling. | Add a Marker input-size/streaming test and verify rejection occurs before `read_bytes()`/request construction. | fixed | 3 |
| 59 | tests/test_formats.py | 631 | Medium | Testing & Validation | The “marker not in auto” gate searches source text for `_marker`. Auto could invoke Marker indirectly through `_EXTRACTORS`, an alias, or a wrapper and the test would pass. It is also brittle under harmless refactoring. | Patch every auto-tier seam, make Marker raise/record if called, execute `tier="auto"`, and assert Marker received no call. | fixed | 3 |
| 60 | tests/test_formats.py | 635 | Medium | Testing & Validation | Marker’s exclusion from `auto` is tested by searching source text for `_marker`. A dynamic `_EXTRACTORS[name]` call could put Marker into the ladder while this named behavior still passes. | Patch `_marker`/the registry entry with a fail-on-call spy, drive every auto escalation branch, and assert zero calls. | fixed | 3 |
| 61 | tests/test_settings.py | 144 | Medium | Testing & Validation | Invalid-input tests cover selected type errors but omit valid-type invalid domains: unknown tier choices, empty strings, permutation bounds, hurdle lower bounds, and invalid values from arguments. Regressions in `_validate` can therefore pass. | Use a table covering choices, emptiness, both bounds, and every source: argument, environment, file, and injected `file_values`. | fixed | 3 |
| 62 | tests/test_settings.py | 326 | Medium | Testing & Validation | `SecretsStayOut` checks only forbidden substrings in setting and environment-variable names. It passes while credential-bearing URL values are accepted, stored, and printed, so the test does not enforce the behavior its class name and module documentation claim. | Add file and environment tests rejecting URL userinfo and credential-bearing query components, plus output tests ensuring secrets cannot be rendered. | fixed | 3 |
| 63 | tests/test_settings.py | 70 | Medium | Testing & Validation | The “full matrix” exercises precedence only for `ingest.tier`, and merely observes one built-in default. It does not ensure defaults themselves are type/domain validated; production currently inserts defaults without `_coerce` or `_validate`. | Validate every `SPEC` default and add tests with a temporarily invalid default to prove resolution fails closed. | fixed | 3 |
| 64 | eval/extract_bakeoff.py | 12 | Low | Documentation & Knowledge Transfer | The usage example advertises `[-v]`, but no `-v`/`--verbose` argument exists, so following the documented command fails. | Remove `[-v]` or implement and document the option. | fixed | 3 |
| 65 | eval/extract_bakeoff.py | 159 | Low | Maintainability & Readability | `bake_off()` is 94 lines with measured cyclomatic complexity 17; `main()` at line 255 is 61 lines. Both exceed the requested size threshold, and `bake_off()` exceeds the complexity threshold of 15. | Split document measurement, aggregation, resume validation, and rendering into focused typed helpers; extract CLI rendering from argument handling. | fixed | 3 |
| 66 | eval/extract_bakeoff.py | 175 | Low | Redundant & Low-Value Code | When `pdftotext` is among the requested tiers, it is executed once as the control and again as the tier candidate. | Reuse `control_raw` and `control_err` for the `pdftotext` tier. | fixed | 3 |
| 67 | klode/lib/formats/pdf.py | 121,258 | Low | Redundant & Low-Value Code | Docling and Marker duplicate multipart construction, request execution, bounded reading, JSON decoding, and top-level validation. Their validation has already drifted. | Extract a shared bounded multipart-JSON transport helper, leaving backend-specific schema parsing separate. | fixed | 3 |
| 68 | klode/lib/formats/pdf.py | 14 | Low | Redundant & Low-Value Code | `os` is imported but unused; Ruff reports F401. | Remove the import. | fixed | 3 |
| 69 | klode/lib/formats/pdf.py | 156,159,305,307 | Low | Code Correctness & Reliability | Endpoint settings are resolved twice on local/unconfigured paths. A concurrent environment or settings-file change can switch to a remote conversion on the second lookup while the wrapper discards its provenance or follows a different branch. | Resolve configuration once per extraction and pass the resolved endpoint into distinct local/remote helpers. | fixed | 3 |
| 70 | klode/lib/formats/pdf.py | 331 | Low | Maintainability & Readability | `choose_and_extract` is 55 lines and combines forced dispatch, candidate scoring, three-tier orchestration, error translation, and result annotation. Its nested selection helper contributed to inconsistent usability checks. | Split forced extraction, candidate validation/comparison, and automatic orchestration into focused functions. | fixed | 3 |
| 71 | klode/lib/formats/pdf.py | 36 | Low | Documentation & Knowledge Transfer | The `corruption_score` docstring claims `mfonnafion` is detected as a mid-word-cap marker, but that all-lowercase token cannot match `_MISCAP`. | Remove the false example or implement and test a separate signal capable of detecting it. | fixed | 3 |
| 72 | klode/lib/formats/pdf.py | 62,188 | Low | Dependency & Environment Safety | Installation guidance injects unconstrained latest versions of Kreuzberg and Docling even though the adapters depend on specific APIs. Optional environments are therefore irreproducible and can break without repository changes. | Publish tested optional version ranges or compatibility metadata while keeping the base runtime dependency list empty. | fixed | 3 |
| 73 | klode/lib/settings.py | 107 | Low | Redundant & Low-Value Code | `DEFAULTS` is an exported, mutable duplicate of data already held in `SPEC` and has no repository usage. It creates another surface that callers can mutate even though resolution ignores those mutations. | Remove it, derive defaults through a function, or expose an immutable mapping and make resolution use the same source. | fixed | 3 |
| 74 | klode/lib/settings.py | 133 | Low | Documentation & Knowledge Transfer | Exported configuration APIs are incompletely documented: `settings_path()` has no docstring, while `resolve()` does not document `file_values`, `explicit`, `home`, return semantics, or exceptions. The ambiguity is material given the silent explicit-path behavior above. | Add parameter, return, precedence, missing-file, and exception documentation for all exported functions. | fixed | 3 |
| 75 | klode/lib/settings.py | 74 | Low | Redundant & Low-Value Code | `judge.model` and `judge.permutations` are deliberately exposed but have no consumer. Warning users that settings do nothing reduces deception but does not eliminate the dead configuration surface or its maintenance burden. | Remove them until implemented, or wire them into construction of the real judge and add consumer-boundary tests. | skipped (declined, see report) | 3 |
| 76 | tests/test_formats.py | 1 | Low | Documentation & Knowledge Transfer | The module docstring says the only mocks are the Poppler subprocess and lazy OCR seam, but the file also mocks HTTP transport, settings paths, constants, and endpoint behavior. It also claims mocked seams are paired with real paths, while the Poppler “real” path can skip any runtime failure. | Rewrite the test-scope documentation to describe the actual mocked boundaries and integration guarantees. | fixed | 3 |
| 77 | tests/test_formats.py | 124 | Low | Documentation & Knowledge Transfer | The test name says `Extraction` has four fields, but the model has a fifth `pages` field. The assertion omits it, so the stale claim remains green. | Rename the test and assert all dataclass fields, including `pages=None`, plus populated page provenance. | fixed | 3 |
| 78 | tests/test_formats.py | 145 | Low | Testing & Validation | The protocol-surface test checks only attribute existence. Handlers with `sniff = None` or `extract = None` pass. | Assert `isinstance(handler, FormatHandler)`, callability, and representative callable behavior/signatures. | fixed | 3 |
| 79 | tests/test_formats.py | 233 | Low | Redundant & Low-Value Code | The first EPUB assertion is short-circuited whenever sniffing the temporary directory returns `False`, so its valid-EPUB branch normally never runs. Lines 241–242 already test both outcomes directly. | Delete the compound assertion and retain explicit, unconditional positive and negative cases. | fixed | 3 |
| 80 | tests/test_formats.py | 299 | Low | Redundant & Low-Value Code | `test_namespace_resolved` duplicates the immediately preceding exact `Run1Run2` behavior with a weaker `assertIn`. | Remove it or replace it with a genuinely distinct namespace edge case. | fixed | 3 |
| 81 | tests/test_formats.py | 402 | Low | Testing & Validation | The candidate-retention test covers only empty OCR. It misses short nonempty output and equal-score replacement—the actual defective branches. | Add tests for one-word OCR, output below `MIN_WORDS`, equal scores, and preservation of the previous candidate and Tier 3 escalation. | fixed | 3 |
| 82 | tests/test_settings.py | 223 | Low | Dependency & Environment Safety | Discovery tests resolve the caller’s real environment and home settings. An unrelated invalid local setting can make these output-format tests fail or change their output. | Patch `settings_path` to an isolated absent file and clear all `SPEC.env` variables around `_explain`. | fixed | 3 |
| 83 | tests/test_settings.py | 232 | Low | Testing & Validation | `test_every_spec_appears_with_its_help_and_env` checks only approximately the first 40 characters of each help string and does not bind that text to the corresponding rendered setting. Most documentation could be deleted, truncated, or attached to the wrong key while the test still passes. | Parse the output per setting and assert each complete help text and metadata block, or test structured explanation data before formatting. | fixed | 3 |
| 84 | tests/test_settings.py | 232 | Low | Testing & Validation | An empty `spec.help` produces an empty search string at line 237, and `assertIn("", out)` always passes. Thus the test named “with its help” does not require help to exist. | Assert `spec.help.strip()` first, then match a nonempty normalized help fragment. | fixed | 3 |
| 85 | tests/test_settings.py | 44 | Low | Maintainability & Readability | Environment save/clear/restore logic is copied almost verbatim again at lines 111–129. The two versions can drift and make test isolation unreliable. | Extract a shared base fixture/context manager using `patch.dict` with restoration. | fixed | 3 |
| 86 | klode/lib/cli.py | 141 | Critical | dim3 | `cmd_check` prints `OK` and exits 0 whenever `r.errors` is empty, even when zero corpus sources caused the citation/source checks to abstain. This converts unmeasured evidence into a confident success verdict. | Add an explicit measured/abstained report state; print `ABSTAINED` and return nonzero unless missing evidence was explicitly permitted. | fixed | 1 |
| 87 | klode/lib/cli.py | 129 | High | dim3 | Explicit `--entail` silently degrades backend absence or initialization failure to a note, then `cmd_check` can print `OK` and exit 0 without performing the requested measurement. | Return a nonzero unavailable/abstained result when the explicitly requested backend cannot run; distinguish scoring failures from ordinary low-support warnings. | fixed | 2 |
| 88 | klode/lib/cli.py | 627 | High | dim2 | `--entail-threshold` accepts NaN, infinities, and values outside `[0,1]`. With NaN, every `support < threshold` comparison is false; a fake backend returning `0.0` scored four anchors as “0 below” and the CLI printed `OK`. | Use an argparse validator requiring `math.isfinite(value)` and `0 <= value <= 1`, with defense-in-depth validation in `check()`. | fixed | 1 |
| 89 | klode/lib/cli.py | 156 | High | dim2 | User-controlled `--stamp` is passed directly into the backup path. Values containing enough `../` components escape the configured backup root and can make normalization copy files outside it. | Restrict stamps to a single safe path component and independently resolve/check that the resulting backup directory remains under `backup_root`. | fixed | 1 |
| 90 | klode/lib/cli.py | 226 | High | dim2 | Zoom/verify trust a card’s `file:` through `query.source_of`, which confines it only to `cfg.root`, not to configured shelf `.txt` paths. A malicious card can point at `.env`, `.git/config`, or another repository file and expose its full line by matching a known prefix. | Require sources to resolve as `cfg.lib/<configured-shelf>/<name>.txt`; use that validator for both zoom and the verify service. | fixed | 1 |
| 91 | klode/lib/cli.py | 235 | High | dim3 | Prose `zoom --level content --grep` calls occurrence-only `query.verify`, bypassing stamped-source freshness and ambiguity checks. JSON routes the same request through `services.execute("verify")`, so stale/ambiguous evidence can succeed only in prose. | Remove the legacy direct path and render the shared structured `EvidenceHit`. | fixed | 3 |
| 92 | klode/lib/cli.py | 173 | High | dim3 | `normalize --check` returns success when files were skipped, including invalid UTF-8, and when no files matched. Thus the gate can pass without checking the corpus. | Make skipped/unreadable inputs and an unexpectedly empty match set nonzero in check/apply modes; expose an explicit abstained state. | fixed | 2 |
| 93 | klode/lib/cli.py | 227 | High | dim3 | Requesting L3 content for an uninstalled source prints an unavailable message but exits 0, unlike a missing card or section. | Return nonzero and propagate the same unavailable resolution through JSON. | fixed | 2 |
| 94 | klode/lib/cli.py | 108 | High | dim3 | `build()` reporting `skipped` causes `cmd_build` to return 0. Automation therefore records a successful build despite no corpus and no refresh. | Preserve the artifacts, but report `ABSTAINED/SKIPPED` with a nonzero status unless explicitly allowed. | fixed | 2 |
| 95 | klode/lib/cli.py | 89 | High | dim2 | Once any klode marker exists in `.gitignore`, initialization never refreshes the managed rules. Re-running `init --force` with new shelves leaves their copyrighted `.txt`/`.pdf` files unignored. | Use delimited begin/end markers and atomically replace the managed block on every initialization. | fixed | 1 |
| 96 | klode/lib/cli.py | 451 | High | dim3 | `_json_exit` guesses success from Python container types rather than operation semantics. A missing zoom section returns JSON `body:null` with exit 0 while prose exits 1; an empty registry exits 1 in JSON while prose deliberately exits 0. Fan-out failures can also return 0. | Put an explicit success/outcome field in `OpResult`, or define exit policy per operation. | fixed | 3 |
| 97 | tests/test_lodlib.py | 167 | High | dim7 | `test_absent_corpus_degrades_not_fails` explicitly locks in the fail-open state that lets the CLI report `OK` for unmeasured source-dependent checks. | Change the contract to an explicit abstained result and add a CLI test asserting nonzero exit and no `OK` text. | fixed | 1 |
| 98 | klode/lib/cli.py | 75 | Medium | dim2 | Initialization follows pre-existing `library`, `library.toml`, and `.gitignore` symlinks. In an attacker-prepared target directory this can create or overwrite files outside the requested root. | Reject symlinked managed paths/components and use no-follow, atomic file creation/replacement. | fixed | 3 |
| 99 | klode/lib/cli.py | 601 | Medium | dim3 | Global `--json` is accepted for every subcommand, but `check`, `build`, `normalize`, `init`, `ingest`, and `settings` ignore it and emit prose. `klode --json check` was confirmed to produce non-JSON output. | Implement JSON for all accepted commands or expose the option only on supported read subparsers. | fixed | 3 |
| 100 | klode/lib/cli.py | 182 | Medium | dim3 | The JSON search branch runs before prose’s nonblank-query validation. `--json search ""` returns a successful empty result while prose rejects the same input. | Validate input before selecting the renderer, preferably in the shared service. | fixed | 3 |
| 101 | klode/lib/cli.py | 669 | Medium | dim3 | `--limit` accepts zero and negative values. Prose uses Python slicing directly, so `--limit 0` falsely says “no matching cards” and `-1` drops the last result; JSON silently clamps both to one. `--max` has analogous nonpositive behavior. | Reject nonpositive/out-of-range values at parse time and use identical shared validation on every surface. | fixed | 3 |
| 102 | klode/lib/cli.py | 660 | Medium | dim3 | `--apply` and `--check` are not mutually exclusive. When combined, normalization mutates files and line 173 silently disables the promised gate behavior. | Put them in a mutually exclusive group or reject the combination explicitly. | fixed | 3 |
| 103 | klode/lib/cli.py | 214 | Medium | dim3 | `--grep` and `--max` are accepted with meta/thin/full levels but silently ignored. An empty `--grep ""` at content level is also treated as if verification was not requested and exits successfully. | Validate that grep/max are content-only and reject an explicitly supplied blank phrase. | fixed | 3 |
| 104 | klode/lib/cli.py | 193 | Medium | dim2 | Card gists, titles, framework text, and raw source lines are printed without removing terminal control sequences. Untrusted corpus/card data can inject ANSI/OSC commands, including terminal spoofing or clipboard operations. | Escape unsafe control characters for TTY prose output while leaving JSON structurally encoded. | fixed | 3 |
| 105 | klode/lib/cli.py | 418 | Medium | dim9 | The architecture comment says consumption verbs route through `services.execute`, but prose search, zoom, consult, and diagnose call `query`/`console` directly. The observed stale-verification and limit divergences prove the statement false. | Route both renderers through the shared service or correct the documented architecture. | fixed | 3 |
| 106 | tests/test_path_safety.py | 40 | Medium | dim7 | The path-safety test varies only the card ID. It never creates a valid card whose `file:` points to a sensitive in-root, out-of-shelf file, so the source disclosure remains undetected. | Add CLI/service tests for `.env`, `.git/config`, non-`.txt`, and non-shelf paths. | fixed | 1 |
| 107 | tests/test_parity.py | 35 | Medium | dim7 | The “parity” gate compares only subcommand names. It still passes if routing through `services.execute` is deleted or prose and JSON semantics diverge completely. | Compare structured values and exit statuses across prose/JSON for missing, stale, ambiguous, empty, and invalid inputs. | fixed | 3 |
| 108 | tests/test_cli_agentic.py | 121 | Medium | dim7 | Initialization tests cover only unsafe shelf rejection. Successful scaffold, forced refresh, managed `.gitignore` replacement, symlink handling, and partial-write behavior are untested. | Add isolated filesystem tests for the complete success/update/security paths. | fixed | 1 |
| 109 | klode/lib/cli.py | 125 | Low | dim3 | `--entail-model` and `--entail-threshold` are silently ignored unless `--entail` is also supplied. | Reject dependent options without `--entail`, or make either imply it. | fixed | 3 |
| 110 | klode/lib/cli.py | 562 | Low | dim6 | `sys.stdin.read()` eagerly accepts an unbounded draft, allowing accidental or adversarial memory exhaustion. | Stream with a documented byte/character limit and fail clearly when exceeded. | fixed | 3 |
| 111 | klode/lib/cli.py | 347 | Low | dim1 | The consult projection decision tree is copied verbatim into both JSON and prose branches. | Extract a single projection/sections helper and feed both renderers from it. | fixed | 3 |
| 112 | klode/lib/cli.py | 594 | Low | dim5 | `build_parser` is 127 lines and manually duplicates the operation registry, making option dependencies and surface drift difficult to maintain. | Split parser construction per command and derive shared operation metadata from `opspec`. | fixed | 3 |
| 113 | klode/lib/cli.py | 470 | Low | dim9 | `_run` claims it “emit[s] JSON,” but it only returns an `OpResult`; callers emit it separately. | Correct the docstring. | fixed | 3 |
| 114 | klode/lib/cli.py | 618 | Low | dim9 | `--quiet` promises “only problems + summary,” but `cmd_check` still prints every note, including non-problem informational notes. | Suppress notes in quiet mode or update the help text. | fixed | 3 |
| 115 | tests/test_cli_agentic.py | 128 | Low | dim7 | The cards/lenses CLI test ignores both return codes, so it passes if either command prints partial output and then reports failure. | Assert exit 0 and validate empty/error cases separately. | fixed | 3 |
