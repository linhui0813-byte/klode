# Audit Findings

**Run**: audit-fix 20260811-145435 | **Scope**: plan-v2 implementation (commit 19465de) | **Audit type**: full (9-dim)
**Model**: gpt-5.6-sol | **Effort**: high | **Rounds requested**: 3
**Status values**: open | fixed | not-fixed | partial | regressed

Merged from two audit groups (core modules; integration + harness + tests). Lower-severity
duplicates and pure-style findings are folded into the rows below.

| # | File | Line | Severity | Dimension | Finding | Suggested fix | Status | Round |
|---|------|------|----------|-----------|---------|---------------|--------|-------|
| 1 | klode/lib/agreement.py | 39 | High | Correctness | `_NONWORD=[^0-9a-z]+` drops ALL non-ASCII. Two entirely different CJK/Cyrillic texts tokenize to the same ASCII residue -> containment/inflation/order all 1.0 -> `verified`. Also corrupts visual.py, which reuses the tokenizer. | Unicode-aware tokenization via str.isalnum(); multilingual regression tests. | fixed | 1 |
| 2 | klode/lib/agreement.py | 135 | High | Correctness | 'Per-page' order has no page boundaries — it uses fixed 400-token windows. Reverse every 40-token page: all windows score ~0.98 and decide() returns `verified`. The 10/10-at--1 guarantee only holds when pages are exactly 400 tokens aligned to offset 0. | Accept real page boundaries and window WITHIN each page; otherwise drop the per-page claim. | fixed | 1 |
| 3 | klode/lib/coverage.py | 51 | High | Correctness | docling's top-level `pages` map is SOURCE-page metadata, not evidence content was extracted. `{'pages':{1,2,3},'texts':[prov 1, prov 3]}` returns (1,2,3), masking missing page 2. | Derive coverage from content `prov` only; track source inventory separately. | fixed | 1 |
| 4 | klode/lib/integrity.py | 84 | High | Correctness | An Agreement whose every order window ABSTAINED still returns `verified` when containment/inflation pass. Order-unmeasurable text is confidently accepted. | Require order to have been measured before returning verified; else abstain. | fixed | 1 |
| 5 | klode/lib/integrity.py | 124 | High | Correctness | Coverage-only with `candidate_known=False` returns `verified` — a Coverage object existing is treated as candidate evidence. | Return abstained unless agreement OR candidate-known coverage supplied real evidence. | fixed | 1 |
| 6 | klode/lib/visual.py | 148 | High | Correctness | Recall uses a SET of candidate tokens, so it is order- AND repeat-insensitive. OCR 'a b c' vs candidate 'c b a a a' scores 1.0. The scrambling failure this module exists to catch scores perfect. | Counter multiset intersection; add an order-sensitive signal. | fixed | 1 |
| 7 | klode/lib/integrity.py | 107 | High | Correctness | Only MEDIAN order gates. One fully reversed page in ten: worst=-1, median=1 -> `verified`, contradicting the docstring that one bad page must not be buried. | Gate on worst/p05 or a bad-window count as well. | fixed | 1 |
| 8 | klode/lib/integrity.py | 52 | High | Correctness | `Integrity` accepts arbitrary state strings; `Integrity('faield').blocks_write` is False — a typo fails OPEN. Thresholds default to {} so a verdict can carry none. | Validate state in __post_init__; require thresholds. | fixed | 1 |
| 9 | klode/lib/ingest.py | 236 | High | Correctness | Transactional claim is false AFTER promotion: os.replace mutates the shelf, then provenance append can fail (unwritable/dir) -> failure + shelf mutation. | Write provenance before promotion, or roll back the artifact on provenance failure. | fixed | 3 |
| 10 | klode/lib/ingest.py | 203 | High | Security | Fixed `<dest>.tmp` name: a pre-planted symlink at that path is followed by write_text and truncates the victim. Same defect class already fixed in gate/__main__.py. | tempfile.mkstemp in the destination dir, then os.replace. | fixed | 1 |
| 11 | klode/lib/formats/pdf.py | 133 | High | Correctness | `_LAST_PAGES` module-global keyed by path: same-path concurrency can attach one extraction's coverage to another; entries leak when docling loses. | Return pages with the Choice; delete the global carrier. | fixed | 3 |
| 12 | klode/lib/formats/pdf.py | 99 | High | Correctness | `to_formats` sent as one value 'md,json'; docling models it as a LIST, so the request can be rejected and JSON provenance never obtained. | Repeat the multipart field: to_formats=md and to_formats=json. | fixed | 1 |
| 13 | eval/extract_bakeoff.py | 147 | High | Correctness | The harness never ranks. It prints tiers in insertion order with no per-backend aggregate — deleting 'ranking logic' would change nothing because none exists. | Aggregate visual per backend, sort, expose ordered ranking. | fixed | 3 |
| 14 | eval/extract_bakeoff.py | 91 | High | Correctness | docling markdown has no form feeds, so the backend the harness exists to evaluate always gets visual=None and cannot be ranked. | Build page text from structured provenance. | fixed | 3 |
| 15 | tests/test_ingest_integrity.py | 216 | Medium | Testing | `unittest.main()` sits BEFORE VerifyExtractionOnRealText — direct execution silently omits 5 tests. THIRD occurrence of this defect class in this repo. | Move the main guard to the end; add a repo-wide check. | fixed | 1 |
| 16 | tests/test_coverage.py | 42 | High | Testing | The test BLESSES the docling `pages` map as candidate coverage, encoding finding #3's false positive. | Assert pages 1..3 with prov only 1,3 leaves page 2 unrepresented. | fixed | 1 |
| 17 | tests/test_ingest_integrity.py | 64 | High | Testing | No verdict test for all-windows-abstained or one-corrupt-window; both fail-open states pass unnoticed. | Add both as explicit not-verified assertions. | fixed | 1 |
| 18 | tests/test_pdf_corpus.py | 53 | Medium | Testing | Regeneration test rewrites tracked fixtures in place then compares to copies — removing every write_pdf() call still passes. | Generate into an empty temp dir; compare the full file set. | fixed | 2 |
| 19 | tests/test_pdf_corpus.py | 73 | Medium | Testing | Declared tokens are checked anywhere in the whole document, not on their declared page — moving all text to page 1 still passes. | Split by page; assert per page and column order. | fixed | 3 |
| 20 | tests/test_bakeoff.py | 65,79,90 | Medium | Testing | Absent-backend test depends on docling NOT being installed (fails on a provisioned machine); sampled-page assertion is conditional so it can execute nothing; ranking test supplies ONE tier. | Mock backends; assert unconditionally; two backends with reversed scores. | fixed | 3 |
| 21 | klode/lib/settings.py | 162 | Medium | Correctness | `file_values=` bypasses unknown-key validation, contradicting the public guarantee. | Validate injected file_values identically. | fixed | 1 |
| 22 | klode/lib/settings.py | 139 | Medium | Correctness | No domain validation: tier 'bogus', hurdle 999, permutations 0, empty model all accepted despite documented ranges. | Per-spec validators applied to arg/env/file uniformly. | fixed | 1 |
| 23 | klode/lib/settings.py | 47 | Medium | Redundant | The three judge settings are consumed nowhere — the module overclaims that it configures the judge. | Wire them into judge construction or narrow the docs. | fixed | 1 |
| 24 | klode/lib/coverage.py | 72 | Medium | Correctness | `pages_from_docling({'texts':[{'prov':1}]})` raises TypeError — not total. | Require prov to be a list. | fixed | 1 |
| 25 | klode/lib/visual.py | 135 | Medium | Correctness | TemporaryDirectory() is outside the handler; its failure escapes instead of becoming a recorded skip. The auditor HIT this (5 tests errored). | Catch OSError around creation; return a skipped report. | fixed | 1 |
| 26 | klode/lib/ingest.py | 224 | Medium | Correctness | output_sha256 hashes cleaned.encode('utf-8') while the file is written in TEXT mode — newline translation makes the hash disagree with disk on Windows. | Encode once, write_bytes, hash those exact bytes. | fixed | 1 |
| 27 | klode/lib/formats/pdf.py | 109 | Medium | Correctness | A valid non-object JSON response (e.g. `[]`) reaches data.get and raises AttributeError instead of RuntimeError. | isinstance(data, dict) check. | fixed | 1 |
| 28 | eval/extract_bakeoff.py | 76 | Medium | Documentation | Claims to rank 'against ground truth' but never loads GROUND-TRUTH.json. | Load and score labeled truth, or drop the claim. | fixed | 3 |
| 29 | tests/test_settings.py | 49 | Medium | Testing | setUp deletes all KLODE_* env vars without restoring — contaminates later tests and the caller's environment. | Save and restore. | fixed | 3 |
| 30 | klode/lib/cli.py | 655 | Low | Redundant | `--sources` is a no-op by default and `--effective` hides sources despite its help. | Single clear mode. | fixed | 2 |
