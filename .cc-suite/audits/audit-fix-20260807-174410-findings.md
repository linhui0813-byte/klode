# Audit Findings

**Run**: audit-fix 20260807-174410 | **Scope**: CriterionSpec v1 slice (`klode/gate/*`, `klode/lib/*` diff, `eval/rate.py`, new tests + docs) | **Audit type**: full (9-dim)
**Model**: gpt-5.6-sol | **Effort**: high | **Audit threads**: `019fdb7f-ad91-7260-be3d-52c1d758bdbd` (core), `019fdb87-952d-7573-b6ed-5ef350ffc766` (integration), `019fdb8e-3295-79b0-be32-71f27e9f993a` (harness/tests/docs)
**Status values**: open | fixed | not-fixed | partial | regressed | wont-fix (rationale) | skipped

Findings marked **[V]** were reproduced locally before being accepted. Duplicates across the three
audit groups are merged into one row.

| # | File | Line | Severity | Dimension | Finding | Suggested fix | Status | Round |
|---|------|------|----------|-----------|---------|---------------|--------|-------|
| 1 | klode/gate/spec.py | 332 | High | Correctness | **[V]** `explicit` accepts `FOLDED_ONLY`, so "verbatim" is not verbatim. Source `re-sign` grounds the explicit value `resign`; `Keep—this` grounds `Keep-this`. The central guarantee of the artifact is defeated by the normalizer. | Exact raw-substring check for `explicit` values; never accept folded equivalence as verbatim. | fixed | 2 |
| 2 | klode/gate/review.py | 109 | High | Correctness | **[V]** Review loads with `corpus=False`, skipping fingerprint AND explicit-value grounding. An explicit statement whose quote rotted, or a rubric whose corpus moved, still scores Go/Recycle. Contradicts the module's "current evidence"/"corpus-pinned" claim. | Check fingerprint + every scoring-relevant field at review time, converting failures to per-criterion `Unavailable` rather than skipping them. | fixed | 1 |
| 3 | klode/gate/review.py | 121 | High | Correctness | **[V]** `to_criterion()` drops the parallel `cards`, so `ground()` searches the whole panel. A citation declared to `structure` silently grounds in `brevity`. Per-citation provenance is lost; a rotted citation migrates instead of failing. | Preserve marker↔card pairs; verify each anchor only against its declared card. | fixed | 1 |
| 4 | klode/lib/common.py | 217 | High | Correctness | **[V]** A `before`/`after` pin suppresses ambiguity merely by existing: `Marker("target", before="alpha")` over `"alpha target one; alpha target two"` → `count=2, ambiguous=False`. Pre-existing in the literal branch; mirrored into the regex branch by the earlier fix. | After context filtering, report `ambiguous = len(matches) > 1`; only `#n` unconditionally selects one. | fixed | 1 |
| 5 | klode/gate/spec.py, klode/gate/__main__.py | 343, 31 | High | Security | **[V]** `dimension` is interpolated into a path with no containment check. `_target()` resolves `../escape` outside `cfg.criteria`; `derive`/`repin` then write there. `_svc_review` accepts `dimension` from service params. | Require a safe stem (no separators, no `..`, no absolute); verify the resolved path is under `cfg.criteria`. | fixed | 1 |
| 6 | eval/rate.py | 143 | High | Correctness | **[V]** An undefined QWK (constant raters, single item) prints "UNDER-SPECIFIED" per row but leaves `worst` at 1.0, so the run reports "applied consistently" and exits 0 — a self-contradicting report. | Treat `qwk is None` as failure; report undefined separately from disagreement. | fixed | 1 |
| 7 | eval/rate.py | 115 | High | Correctness | **[V]** Rows unfilled in BOTH sheets vanish from the key intersection, so `missing` is empty and a partial sheet reports clean agreement. | Require identical complete inventories; fail on any unfilled or absent row. | fixed | 1 |
| 8 | eval/rate.py | 108 | High | Correctness | Sheet identity is never checked: `score alice.jsonl alice.jsonl` certifies one rater as inter-rater agreement; two rubric revisions can be compared if keys overlap. | Add a sheet header (schema, rater, rubric digest, draft-set digest); reject equal raters and mismatched headers. | fixed | 2 |
| 9 | eval/rate.py | 136 | High | Correctness | `k` comes only from rater A's editable `max_score`; a missing `max_score` silently becomes 10; a B score above A's k raises `IndexError`. Incompatible scales can report perfect agreement. | Require both sheets to declare the same scale per criterion; validate every score against it. | fixed | 2 |
| 10 | dev-docs/SPEC-criterion.md | 43 | High | Documentation | The claim that the motivating contradiction is "unrepresentable" is false — it is unrepresentable as `explicit`, but can still be authored as `paraphrase` with any non-empty warrant, since warrant *presence* is checked, not entailment. | State the guarantee precisely: a contradiction cannot masquerade as quotation; as inference it still requires human review. | fixed | 1 |
| 11 | klode/lib/services.py | 315 | High | Correctness | `_locate_folded()` receives only the phrase and ignores `before`/`after`/`#n`, so a folded, context-pinned match shows the judge an unrelated earlier occurrence. Pre-existing. | Pass the full `Marker` to folded location, or reject the evidence as unshowable. | fixed | 2 |
| 12 | eval/rate.py | — | High | Testing | No test covers `_qwk`, `cmd_score`, `cmd_sheet`, ordering, coverage, or malformed rows. Every rating-harness defect above is unguarded. | Add `tests/test_rate.py` with known kappa matrices, degenerate cases, partial sheets, same-rater rejection, scale mismatch, and exit codes. | fixed | 1 |
| 13 | klode/gate/review.py | 156 | Medium | Correctness | **[V]** Percentages are rounded per criterion then averaged. Maxima (2,3) with scores (0,2): implementation 34, exact 33 — flips Recycle→Go at hurdle 34. | Average exact ratios (`Fraction`), round once; compare defects as `score*100 < hurdle*max_score`. | fixed | 1 |
| 14 | klode/gate/judge.py | 47 | Medium | Documentation | **[V]** `round()` is ties-to-even: `default_fraction=0.9` on 0..5 gives 4, not the 5 the docstring claims. The review service's 0.7 gives 4/5 = 80%. | Use explicit half-up quantization, or correct the docstring to the attainable value. | fixed | 2 |
| 15 | klode/gate/spec.py | 144, 190, 246, 281, 316 | Medium | Correctness | Malformed JSON escapes the promised `SpecError` boundary as `AttributeError`/`TypeError`: `warrant:1`, `before:1`, `nth:"1"`, `fields:[1]`, non-string digests. Falsey wrong types are silently erased (`evidence:""`→`[]`, `fields:[]`→`{}`). `nth:true` is accepted as 1. | Type-validate every property before normalization; never use `or`-default to erase a supplied wrong type. | fixed | 2 |
| 16 | klode/gate/spec.py | 359 | Medium | Correctness | `load()` wraps only `JSONDecodeError`; invalid UTF-8 (`UnicodeDecodeError`), read failures (`OSError`), and deep nesting (`RecursionError`) escape. Duplicate JSON keys are silently last-wins, so `{"admission":"candidate","admission":"human_approved"}` reads as approved. | Wrap decode/read failures in `SpecError`; reject duplicate keys via `object_pairs_hook`. | fixed | 1 |
| 17 | klode/gate/spec.py | 316 | Medium | Correctness | Fingerprint digests are not format-checked: an int reaches `pinned[:12]` and raises `TypeError`; malformed strings are misdiagnosed as drift. | Require `^[0-9a-f]{64}$` at parse time. | fixed | 1 |
| 18 | klode/gate/spec.py | 260 | Medium | Correctness | Duplicate panel cards survive trimming (`["brevity"," brevity "]`), then runtime grounding checks the same card twice and reports `ambiguous-panel`, making a validated rubric permanently unavailable. | Normalize then reject duplicates at parse time. | fixed | 1 |
| 19 | klode/gate/spec.py | 225 | Medium | Standards | `^C\d+$` rejects only that exact shape; `c1`, `C1a`, `1`, `pacing.1`, and control-character/newline ids all pass (the last can corrupt `rate.py`'s table). | Enforce a stable-id grammar: no separators, controls, whitespace, or numeric-only. | fixed | 1 |
| 20 | klode/gate/authoring.py | 75 | Medium | Correctness | `derive` assigns every marker to the FIRST grounded card. In the shipped pacing fixture `slows the tempo` resolves in `structure` but is cited to `brevity`. If grounding fails entirely it fabricates `panel[0]`. | Consume `g.anchors` marker-by-marker; fail derivation rather than inventing a card. | fixed | 1 |
| 21 | klode/gate/__main__.py | 76, 36 | Medium | Correctness | `repin` rewrites raw JSON with no schema validation (non-object JSON raises `AttributeError`). Writes are non-atomic: `repin` can truncate the only approved rubric; `derive` has a check-then-write race. | Validate before re-pinning; exclusive-create for derive, tmp+`os.replace` for repin. | fixed | 2 |
| 22 | klode/gate/review.py | 146 | Medium | Correctness | Judge scores are range-checked but not type-checked: `0.6` and `True` pass, matching no discrete behavioral level. | Require a non-bool `int`. | fixed | 1 |
| 23 | klode/gate/spec.py | 262 | Medium | Security | `admission` is an unauthenticated string in the same agent-editable file, and nothing binds approval to the content approved — a rubric can be approved, then edited. | Bind approval to a canonical content digest and verify it before scoring; document the honor-based trust boundary accurately. | fixed | 2 |
| 24 | klode/gate/criteria.py, spec.py | — | Medium | Redundant | `criticality: advisory` is parsed and carried but has no behavioral effect — advisory criteria gate the verdict exactly like required ones, misleading rubric authors. | Reject `advisory` in a canonical spec until the semantics exist. | fixed | 1 |
| 25 | eval/rate.py | 184, 92, 148, 39 | Medium | Correctness | `--bar nan` makes every row "under-specified" yet exits success; `score`/`max_score` coerced with `int()` (floats truncate, bools pass, negatives index from the end, huge `max_score` allocates k²); OVERALL pools raw categories across heterogeneous scales; draft identity is `Path.stem`, so `x.md`/`x.txt` collide. | Validate the bar as finite 0..1; validate scores as non-bool ints in range; drop OVERALL kappa for mixed scales; reject duplicate stems/keys. | fixed | 2 |
| 26 | klode/lib/config.py | 156 | Medium | Environment | `[frameworks].criteria` accepts absolute paths and `..`, so canonical rubric loading can be relocated outside the framework tree. | Reject absolute/escaping values; require containment under `frameworks`. | fixed | 1 |
| 27 | tests/test_criterion_spec.py | 132, 305 | Medium | Testing | `test_unknown_takes_no_warrant_or_evidence` only supplies a warrant — it passes if the evidence half is deleted. `test_a_derived_candidate_does_not_validate` is mutation-vacuous: the derived doc has many simultaneous errors, so deleting any one rule still raises. Several validator branches have no test. | Split the unknown test; repair-all-but-one for derive; add one mutation per validator branch. | fixed | 1 |
| 28 | klode/gate/spec.py | 157 | Medium | Correctness | Zero-width characters survive `strip()`, so `value:"​"` and `warrant:"​"` satisfy the non-empty checks — a scale can validate with no usable descriptor. | Require visible content; reject control/format-only text. | fixed | 1 |
| 29 | klode/gate/spec.py | 281, 326 | Medium | Correctness | `dimension` is never validated and is coerced with `str()` (lists, `null`, bools accepted). An `explicit` value is checked only against `cards[0]`, rejecting a valid value cited in a later card and discarding selectors. | Validate `dimension` as a non-empty safe string; check explicit values against their own evidence binding. | fixed | 2 |
| 30 | klode/gate/spec.py | 98, 108 | Low | Maintainability | Frozen dataclasses hold mutable `dict`s (`fields`, `fingerprint`), so a validated object can be mutated afterwards. `self.statement.value or self.id` is unreachable for parser-produced criteria and masks invalid direct construction. | Store immutable mappings; use the validated value directly. | fixed | 2 |
| 31 | klode/gate/authoring.py | 81, 87 | Low | Correctness | Derived ids track editable statement text (renaming a move renames the "stable" id); punctuation-only statements all collapse to `criterion`, colliding. `levels` accepts 0/1/negative/bool. | Detect slug collisions; require a non-bool int ≥ 2 at both API and CLI boundaries. | fixed | 1 |
| 32 | eval/rate.py, .gitignore | 76, 1 | Low | Environment | `--inline` repeats each draft once per criterion; rating sheets (`alice.jsonl`) may hold draft text and evaluations but no output dir is git-ignored. | Reference drafts once; document and ignore a ratings output dir. | fixed | 2 |
| 33 | klode/gate/spec.py, review.py | 305, 119 | Low | Performance | Sources are re-read and re-hashed per citation, per field, and again when building context — roughly criteria × anchors × panel whole-file reads. | Cache immutable source snapshots for the duration of one validation/review. | fixed | 1 |
| 34 | klode/lib/common.py | 214 | Medium | Security | A single broad regex still grounds: `Marker(".+", regex=True)` matches once → `found, count=1, ambiguous=False`. The `.*` case is fixed; breadth is not. | Canonical rubrics already ban regex evidence; for the linter's `grep-re:` escape hatch, document the limit precisely rather than invent a heuristic gate. | wont-fix (rationale) | - |
| 35 | klode/gate/spec.py | _must_quote | High | Correctness | **[V] REGRESSION from round 1** — raised on the FIRST folded card, so an explicit value whose exact quote lived in a later cited card was falsely rejected. | Collect folded cards; only fail after every cited card is tried. | fixed | 2 |
| 36 | klode/lib/services.py | _locate_folded | High | Correctness | **[V] REGRESSION from round 1** — the new selector logic folds whitespace only while the resolver also folds hyphenation, so occurrence numbers misalign and `#1` returned the resolver's SECOND match. | Compare the locatable count against `resolve().count`; return None (unusable) when they disagree. | fixed | 2 |
| 37 | eval/rate.py | 258 | High | Correctness | **[V] REGRESSION from round 1** — `scales.pop()` emptied the set that the next line tested, so every run printed "scales differ", including homogeneous ones. | Test a `homogeneous` flag captured before consuming the set. | fixed | 2 |
| 38 | eval/rate.py | _load | Medium | Correctness | Both sheets deleting the SAME row left equal inventories, so the comparison read as complete. | Check the loaded row count against the header's declared `rows`. | fixed | 2 |
| 39 | klode/gate/spec.py | content_digest | Medium | Correctness | A lone escaped surrogate passed `_visible()` and raised `UnicodeEncodeError` from `content_digest`, escaping the SpecError boundary. | Reject unpaired surrogates in `_as_text`; make `content_digest` total via `surrogatepass`. | fixed | 2 |
| 40 | klode/gate/__main__.py | _write | High | Security | **REGRESSION from round 2** — the temp name was derived from target+PID (predictable) and opened with plain `"w"`, so a pre-planted symlink there truncates its target; `os.link` also fails outright on link-less filesystems. | `tempfile.mkstemp` in the target dir (O_EXCL, 0600, unpredictable), then link; fall back to O_EXCL create with a printed warning when links are unsupported. | fixed | 3 |
| 41 | eval/rate.py | rubric_identity | High | Correctness | `rubric_digest` concatenated free prose with `:` and `/` separators, so statement `a:b`+guidance `c` collided with statement `a`+guidance `b:c`, and `None` collided with `"None"`; selectors and field-level evidence were omitted. | Hash canonical JSON of a structured identity object covering every field, selector, and level. | fixed | 3 |
| 42 | klode/gate/spec.py | load | Medium | Correctness | A 5000-digit integer trips the int digit-conversion limit, raising a bare `ValueError` that `except JSONDecodeError` did not catch. | Catch `ValueError` (JSONDecodeError is a subclass), re-raising SpecError from the duplicate-key hook first. | fixed | 3 |
| 43 | klode/gate/spec.py | content_digest, _parse_criterion | Medium | Correctness | `content_digest` could raise TypeError/ValueError on mixed key types or unserializable values; non-string `fields` keys were accepted. | Wrap canonicalization in SpecError; require visible string field names. | fixed | 3 |
| 44 | eval/rate.py | _load | Medium | Correctness | The header row-count guard ran only when `rows` was already a non-bool int, so a missing / `"3"` / `null` / `true` value skipped the deletion check entirely. | Require `rows` to be a non-bool integer. | fixed | 3 |
| 45 | klode/gate/spec.py | _freeze | Low | Maintainability | "Deep-frozen" froze one level; lists and dicts nested under a fingerprint extension stayed mutable. | Recursive freeze (dict -> MappingProxyType, list -> tuple). | fixed | 3 |
| 46 | tests/test_criterion_spec.py, tests/test_rate.py | end | Medium | Testing | Both round-2 classes were appended AFTER `if __name__ == "__main__"`, so running either file directly silently skipped them (pytest still collected them, hiding it). | Move the main guard to the end of each file. | fixed | 3 |
| 47 | tests/test_rate.py | 293 | Medium | Testing | `test_a_reworded_rubric_is_not_interchangeable` hand-rebuilt the digest inputs, so it passed even if `cmd_sheet` reverted to hashing ids+maxima. | Exercise the real `rubric_identity()`; add a separator-collision test. | fixed | 3 |
| 48 | tests/ | — | Medium | Testing | No test for the half-up tie (`0.58 x 25`), for `_write`, or for the row-count guard. | Added `Round3`, `AtomicWrites` (incl. a symlink-truncation test), and row-count subtests. | fixed | 3 |
